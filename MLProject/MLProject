name: Workflow-CI

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators:
        type: int
        default: 5
      max_depth:
        type: int
        default: 2
    command: "python modelling.py {n_estimators} {max_depth}"